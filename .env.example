# Copy to .env.local (git-ignored) to configure the external API used
# when the "Use external API" dev toggle in Settings is switched on.
# Has no effect in production builds — see src/api/api.js.

VITE_API_BASE_URL=http://localhost:4000/api
VITE_API_KEY=
